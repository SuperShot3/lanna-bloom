# Copy to .env.local and fill in values. In Vercel, add these in Project Settings → Environment Variables.

# LINE Official Account ID for "Order via LINE" (must be exactly @xxxx from LINE OA manager, e.g. @lannabloom). Not the lin.ee link.
# NEXT_PUBLIC_LINE_OA_ID=@lannabloom

# App URL for the order details link sent to WhatsApp/Telegram. Set to your live site URL (e.g. custom domain). If unset on Vercel, the link uses VERCEL_URL (*.vercel.app). Required so the link works when the user clicks it.
# NEXT_PUBLIC_APP_URL=https://www.your-shop.com

# Orders on Vercel — REQUIRED or order links show "Order not found". Create a Blob store:
# Vercel Dashboard → Your project → Storage → Blob. Add BLOB_READ_WRITE_TOKEN to Production AND Preview (same token) so the store is shared.
# BLOB_READ_WRITE_TOKEN=...

# Optional: secret for admin order actions (list/remove). If set, list and delete require header x-admin-secret.
# Admin page URL: /admin/orders (e.g. https://your-domain.com/admin/orders). Enter this secret on the page to view/remove orders.
# ORDERS_ADMIN_SECRET=your_secret_here

# Order notification email (Resend). Add domain lannabloom.shop in Resend, set SPF/DKIM in Porkbun, then use From: orders@lannabloom.shop.
# RESEND_API_KEY=re_xxxx
# ORDERS_NOTIFY_EMAIL=polovnikov.k.v@gmail.com
# ORDERS_FROM_EMAIL=Lanna Bloom Orders <orders@lannabloom.shop>

# Sanity (required for catalog and /studio)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production

# Sanity write (required for partner registration and partner bouquet uploads)
# In sanity.io/manage → your project → API → Tokens: create an API token (Editor or Admin).
# Use the project’s API token, not a personal/session token — “project user not found” means the token user isn’t in the project.
SANITY_API_WRITE_TOKEN=your_write_token